import printf;

int get_int(int num) {
    printf("%d\n", num);
    return num;
}
bool get_bool(bool bool_in, int num_2) {
    int num;
    if (bool_in) {
        num = 1;
    } else {
        num = 2;
    }
    printf("%d\n", num);
    return bool_in;
}

long get_long(long num) {
    printf("%ld\n", num);
    return num;
}

void main () {
    int a;
    long b;
    int i;

    a = 10;
    b = -10000000L;
    for (i = -4; i < 6; i += 3) {
        a += get_int(i);
        a -= get_int(i) - get_int(a - 3);
        a *= get_int(i) * get_int(a * 5);
        a /= (get_int(i * 21) / get_int(7)) + get_int(15);
        a += get_int((a+1) * 100);
        a %= (get_int(i * 41) % get_int(18)) + get_int(15);

        b += get_long(long(i));
        b -= get_long(long(i)) - get_long(long(a) - 3L);
        b *= get_long(long(i)) * get_long(long(a) * 5L);
        b /= (get_long(long(i) * 21L) / get_long(7L)) + get_long(15L);
        b += get_long((long(a)+1L) * 100L);
        b %= (get_long(long(i) * 41L) % get_long(18L)) + get_long(15L);
    }
}