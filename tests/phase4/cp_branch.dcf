void main() {
    int a, b, c, x, y;

    a = 10;
    b = a;

    if (a > 9) {
        x = 0;  // does NOT affect b
    } else {
        y = 1;  // does NOT affect b
    }

    c = b + 5;  // ✅ b is still a copy of a — safe to propagate

    return;
}
